## [GitHub Desktop](https://desktop.github.com) `release-3.5.1`
...with disabled `auto-updater` (works on `macOS 11.X`).
Completely bypass all autoUpdater operations.

### [Context](https://github.com/desktop/desktop/issues/3410)
`AutoUpdate` *breaks* the current version (mainly on old systems).

